SELECT 
  CUSTOMER_ID,
  NAME,
  EMAIL,
  GENDER,
  AGE,
  SALARY,
  ADDRESS,
  COUNT(*) AS record_count
FROM customer_stream
GROUP BY 
  CUSTOMER_ID,
  NAME,
  EMAIL,
  GENDER,
  AGE,
  SALARY,
  ADDRESS
HAVING COUNT(*) > 1
EMIT CHANGES;
