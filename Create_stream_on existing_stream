CREATE STREAM CUSTOMER_SPEND_STREAM
 WITH (
KAFKA_TOPIC='customer_spend_topic', PARTITIONS=6, REPLICAS=3, VALUE_FORMAT='JSON'
) AS 
SELECT
  CUSTOMER_ID,
  SPEND,
  TRANSACTION_ID
FROM CUSTOMER_TRANSACTIONS_STREAM
EMIT CHANGES;
