CREATE STREAM MERGED_STREAM WITH (KAFKA_TOPIC='merged_topic', VALUE_FORMAT='JSON') AS SELECT
  T.CUSTOMER_ID,
  T.MONTH,
  T.CATEGORY,
  T.SPEND,
  C.SALARY,
  C.GENDER
FROM TRANSACTION_STREAM T
LEFT OUTER JOIN CUSTOMER_STREAM C WITHIN 1 SECONDS
ON T.CUSTOMER_ID = C.CUSTOMER_ID
EMIT CHANGES;
